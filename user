/* Пользователь:
 - логин;
 - пароль;
 - идентификатор.
*/

/* В приложении предполагаются как минимум два типа пользователей: читатели и библиотекари. 
Поэтому сущность пользователя вынесла в отдельный класс. */

class user
{
    public string login;
    public string parol;
    public int id;
    // поля класса

    
    public user(string login, string parol, int id) // конструктор класса с параметрами
    {
        this.login = id;
        this.parol = id;
        this.id = id;
    }
}

